# CONSTITUTION.md template

Project-level non-negotiable principles. Referenced by every agent at every phase.

Generated by `/aiops-setup`. Edit freely — this is your project's anchor.

---

```markdown
# Constitution: <project-name>

## Testing

- Minimum test coverage: <e.g. 80%>
- All public interfaces must have tests
- <e.g. Integration tests for API endpoints>

## Code quality

- <e.g. No raw SQL — use ORM query builders>
- <e.g. All functions under 50 lines>
- <e.g. TypeScript strict mode>

## Architecture

- <e.g. No circular dependencies between modules>
- <e.g. Database migrations must be reversible>
- <e.g. External API calls go through service layer>

## Performance

- <e.g. P95 response time < 200ms>
- <e.g. No N+1 queries>
- <e.g. Bundle size < 100KB gzipped>

## Security

- <e.g. No secrets in code — use env vars>
- <e.g. All user input validated at boundary>
- <e.g. Auth required on all non-public endpoints>

## Process

- <e.g. Conventional commits>
- <e.g. PR review required before merge>
- <e.g. CHANGELOG.md updated per release>
```
